<% content_for :title, "New post" %>

<div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <h1 class="text-4xl font-bold text-gray-900 mb-8">New post</h1>
  
  <%= render "form", post: @post %>

  <div class="mt-6">
    <%= link_to "Back to posts", posts_path, class: "text-sky-600 hover:text-sky-500 font-medium" %>
  </div>
</div>
